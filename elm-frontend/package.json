{
  "devDependencies": {
    "elm-live": "^4.0.1",
    "json-server": "^0.16.3"
  },
  "scripts": {
    "json": "json-server --port 3001 --watch mockdata.json",
    "dev": "elm-live src/Main.elm --proxy-prefix=/api --proxy-host=http://localhost:3001 --hot --dir=output -- --output=output/main.js"
  }
}
